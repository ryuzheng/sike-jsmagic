"use strict";

var _chai = require("chai");

describe("greet", function () {
    it("greets a person", function () {
        _chai.assert.equal(2 + 2, 4);
    }), it("greets a person flirtatiously if drunk", function () {
        _chai.assert.equal(2 + 2, 4);
    });
});