import { assert } from "chai";

describe("greet", ()=>{
    it("greets a person",()=>{
        assert.equal(2 + 2, 4);
    }),
    it("greets a person flirtatiously if drunk", ()=>{
        assert.equal(2 + 2, 4);
    });
});